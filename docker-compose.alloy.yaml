services:
  web:
    image: node:22-bookworm
    working_dir: /workspace
    network_mode: host
    environment:
      IS_ALLOY: ${IS_ALLOY:-false}
      PORT: "3000"
      HOST: "0.0.0.0"
    volumes:
      - .:/workspace
    command: bash -lc "node server.js"
